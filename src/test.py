import numpy as np

# sample1 = np.zeros(shape=(2))
# print(sample1)
# sample2 = np.ones(shape=(2))
# print(sample2)

# recording = np.empty(shape=(0,2))
# recording = np.append(recording, [sample1], axis=0)
# print(recording)
# recording = np.append(recording, [sample2], axis=0)
# print(recording)


newArray = np.empty(0)

newArray = np.append(newArray, 1)
print(newArray)